<div class="card-container">
  {{#each artists}}
  <div class="card">
    <img src={{images.[0].url}} alt="" class="card-img-top">
    <h2 class="artist-name">{{name}}</h2>
    <a href="/albums/{{this.id}}">View Albums</a>
  </div>
  {{/each}}
</div>